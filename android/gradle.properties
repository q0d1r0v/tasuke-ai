org.gradle.jvmargs=-Xmx8G -XX:MaxMetaspaceSize=4G -XX:ReservedCodeCacheSize=512m -XX:+HeapDumpOnOutOfMemoryError
android.useAndroidX=true
# This newDsl flag was added by the Flutter template
android.newDsl=false
# This builtInKotlin flag was added by the Flutter template
android.builtInKotlin=false

# AGP 9.1.0 warns that 36 is the newest compile SDK it was tested against, and
# fails the build on 37 unless the version is acknowledged here. We need 37
# because permission_handler_android's AAR metadata demands it — see the
# comment in app/build.gradle.kts.
android.suppressUnsupportedCompileSdk=37
