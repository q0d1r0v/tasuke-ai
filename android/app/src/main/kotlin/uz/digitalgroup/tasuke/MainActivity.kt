package uz.digitalgroup.tasuke

import io.flutter.embedding.android.FlutterActivity

// Intentionally empty. Every platform capability this app uses reaches Dart
// through a plugin and a port in `lib/core/`; there is no MethodChannel here,
// because anything defined in this file is invisible to every widget test.
class MainActivity : FlutterActivity()
