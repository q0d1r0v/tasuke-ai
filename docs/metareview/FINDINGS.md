# metareview Findings

- mrvf-20260921-071745882098989-pr-ready-branch-10d735e5-003 [high] Review context risk (architecture-reviewer)
- mrvf-20260921-095851961441098-pr-ready-branch-10d735e5-001 [high] Adjudicated review does not cover the working tree (adversarial-review-reviewer)

## Process Overrides

Deliberate exceptions to the review workflow. Pending entries still block CI.

- mrvf-20260921-071745882098989-pr-ready-branch-10d735e5-003 [pending] Review context risk — requested by claude-opus-5 at 2026-09-21T10:51:14Z: The oversize context is tracked build cache, not the change under review. 211 files and 161 MB of CMake output under packages/whisper_ggml/android/.cxx are committed, so the branch diff measures 244 MB whatever was edited; a .gitattributes marking them -diff brings git's own diff down to 13 MB but does not change what metareview measures. Untracking them is a git-index operation the repository owner reserved to themselves. The change itself WAS reviewed: five dimensions over lib/ with 24 adversarial verifications (21 confirmed, 3 refuted), all six blocker findings fixed, and the critical paths executed on real hardware - the bundled speech model installs and whisper transcribes on-device, the GBNF grammar is accepted by llama.cpp against the real 229 MB model, and Stop returns in 6.2s where it previously never returned. Validation receipt: docs/metareview/evidence/verify-2026-09-21.json (flutter analyze clean, 1124 tests green, 9 goldens). [escalation: Needs the repository owner: either 'git rm -r --cached packages/whisper_ggml/android/.cxx' (untracks 211 files / 161 MB of CMake output; .gitignore already covers them, files stay on disk, history unchanged), or 'metareview override grant' for this finding.]
- mrvf-20260921-095851961441098-pr-ready-branch-10d735e5-001 [pending] Adjudicated review does not cover the working tree — requested by claude-opus-5 at 2026-09-21T10:51:33Z: A review-evidence marker can only attest a committed base..HEAD diff; this gate ran with --include-working-tree because the work is uncommitted. The user explicitly reserved all git operations to themselves ('Git'siz qoldiring, men o'zim hal qilaman'), so committing on their behalf is out of scope for me. The content is nonetheless reviewed and verified: flutter analyze clean, 1124 host tests, 9 goldens, plus two device integration tests running the real native stack (whisper transcribes a known clip verbatim; the real 229 MB LFM2 model extracts through the GBNF grammar in 4.5s). [escalation: Needs the repository owner: commit the working tree, then 'metareview review record-lenses --scope pr-ready --base cb7f434 --verdict PASS --mode in-session-emulated' for the new HEAD and re-run the gate.]
